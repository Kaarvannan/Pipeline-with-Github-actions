apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "second_Deployment.fullname" . }}-test-connection"
  labels:
    {{- include "second_Deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "second_Deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
