apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  namespace: kube-system
 
spec:
  type: LoadBalancer   
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 80